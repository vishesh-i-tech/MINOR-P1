# Agent Guidelines & Boundaries

## Project Boundaries & Rules
1. **Strictly Work Within `PHASE 1`**:
   - All development, code files, documentation, and edits must be located inside the `PHASE 1/` directory.
   - Any new files, dependencies, configurations, or modules must go inside `PHASE 1/`.

2. **Forbidden Directory**:
   - **DO NOT read, view, or inspect** the `OPtionaL/` folder or any files within it.

3. **Restricted Files Outside `PHASE 1`**:
   - **DO NOT read or inspect** any root files or files outside `PHASE 1/` (such as `AI CHat explain .txt`, `PS.png`, `Student Retention Intelligence Platform Complete Project Guide.pdf`, etc.) unless the user explicitly asks you to do so.
